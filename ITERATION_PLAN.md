# DLSS5 SaaS 第一迭代设计

目标：让新用户可以稳定注册、上传图片、使用 GPT Image 2 low 生成结果，并在额度耗尽时得到清楚、可恢复的提示。第一迭代优先保证主链路、账目正确和服务可迁移，暂不扩展复杂的营销功能。

## 产品规则（第一版）

| 套餐 | 月度额度 | 每日上限 | 并发 | 单图大小 |
|---|---:|---:|---:|---:|
| Free | 10 次 | 3 次 | 1 | 5 MB |
| Pro | 500 次 | 50 次 | 2 | 10 MB |
| Team | 2,000 次 | 200 次 | 5 | 20 MB |

额度按 UTC 月度周期重置；每日上限按 UTC 日期计算。生成任务提交时原子预扣 1 次，任务失败、超时或被系统取消时自动退回。重试必须使用幂等键，不能重复扣费。Pro/Team 是有限额的付费套餐，不承诺无限使用。

## 迭代 1：可用主链路

### 后端

- 建立 `/api/v1/generate`：校验 Firebase ID token、图片类型/大小/像素、prompt 长度和套餐额度。
- 服务端调用 FAL `openai/gpt-image-2/edit`，固定 `quality: low`、`num_images: 1`；`FAL_API_KEY` 只存在服务端环境。
- 建立 `/api/v1/jobs/:id`：只允许任务所有者查询，返回 `queued/running/succeeded/failed`。
- 使用数据库事务完成额度预扣、任务创建和幂等键约束；失败路径执行退款。
- 记录最小审计字段：`userId, jobId, plan, units, status, providerRequestId, createdAt, completedAt`。日志禁止 prompt 中的敏感内容、图片数据和密钥。
- 增加超时（建议 120 秒）、重试上限（建议 2 次）、并发锁和基础速率限制；FAL 错误转换为稳定的产品错误码。

### 前端

- 上传后显示文件名、尺寸和校验错误；提交按钮在任务期间禁用并显示阶段状态。
- 生成成功显示结果、原图和下载按钮；失败保留原图和重试入口，不重复扣费。
- 额度卡显示“本月剩余 / 今日剩余 / 重置时间”；接近耗尽时提供升级入口。
- 页面刷新后根据 job ID 恢复任务状态；不把额度判断当作安全边界。
- FAL 输出 URL 在展示前校验协议和响应；不再拼接旧 OSS/CDN URL。

### 验收标准

- 新用户注册后能看到正确的 Free 额度；同一用户重复刷新不会重复赠送。
- 未登录用户不能调用生成 API；伪造或过期 token 返回 401。
- 额度为 0、达到日上限、达到并发上限时分别返回明确错误，数据库余额不变。
- 两个并发请求只成功一个预扣；网络重试携带相同幂等键只产生一个任务。
- FAL 成功只扣 1 次；FAL 失败、超时和用户取消按规则退款；页面刷新可继续查看最终状态。
- 真实 JPEG/PNG/WebP 各完成一次生成；超大文件、错误 MIME、超像素图片被拒绝。
- 旧 API key 不出现在浏览器 Network、生产 JS、Git 历史和日志中。

## 迭代 2：账户与付费

- 接入 Stripe 或已选支付渠道：checkout、webhook 签名校验、订阅状态、续费失败和取消。
- 付费成功后由 webhook 改套餐，前端只读取服务端结果；禁止客户端写 `tier`、`credits`、订阅状态。
- 支持账单记录、发票入口、取消后到期降级和退款策略。
- 增加账户页面：当前套餐、额度消耗、重置日期、任务历史和错误状态。

## 迭代 3：可迁移部署

- 将 API、队列和数据库配置集中到后端；Vercel 前端只负责静态站和反向代理。
- 新服务器用 Docker 固定 Node/Python 版本（以实际后端实现为准），配置 HTTPS、健康检查、进程重启和备份。
- FAL、Firebase、对象存储和 CDN 均支持独立替换；提供 staging 环境，不直接用生产数据测试。
- 迁移前导出用户、套餐、额度流水和任务索引；切换期间禁止双写，保留旧服务用于回滚。

## 现在的实现顺序

1. 修复并通过类型检查，建立 API 函数的请求/响应类型。
2. 完成 Firebase token 验证和服务端额度存储；移除客户端直接扣费/签到写余额。
3. 用 FAL 代理替换旧 FLUX/OSS 主链路，并加入幂等、超时、退款。
4. 编写 API 集成测试和一条真实低额度 smoke test；再部署 Preview。
5. 浏览器验证注册、上传、生成、刷新恢复、失败重试和下载，留存请求 ID 与截图。
6. 通过验收后才切换 `www.dlss5nvidia.com` 生产流量，并保留回滚版本。

## 明确暂缓

暂缓复杂模型选择、无限制 Pro、团队邀请、图片历史无限保存、自动训练、原有 GPU/OSS 双链路并行和大规模 UI 重构。先让一条可计费、可追踪、可恢复的 GPT Image 2 low 链路稳定运行。
